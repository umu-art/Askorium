apiVersion: v1
kind: Service
metadata:
  name: ask-encoder
  namespace: askorium
  labels:
    app: ask-encoder
spec:
  selector:
    app: ask-encoder
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
